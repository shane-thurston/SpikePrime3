from hub import port
import runloop
import motor_pair

async def main():
    # Pair motors on port A and B
    motor_pair.pair(motor_pair.PAIR_1, port.C, port.D)
    motor_pair.move_for_degrees(motor_pair.PAIR_1, 360, 0, velocity = 400)

runloop.run(main())
